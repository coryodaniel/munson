require 'spec_helper'

# describe Munson::Model do
#   # model records should have access to collection, which in the case of a Munson::Model should halt subsequent requests
#   # User.includes('addresses').first.addresses # should make one http call
# end
